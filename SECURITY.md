# Security Policy

Report security issues privately through GitHub Security Advisories after the repository is published. Do not open public issues containing OAuth tokens, Dida task data, local paths, or session transcripts.

The extension executes the bundled Dida CLI with the current user's permissions. Review configuration bindings and use a dedicated Dida project.
